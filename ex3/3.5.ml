(* 3.5 *)
(* Different between
    let x = e1 and y = e2;;
    let x = e1 let y = e2;;
*)

(* let x = 2 and let y = x + 3;; *)
(* As and will not apply value before it, this delcaration will throw error *)

(* let x = 2 let y = x + 3;; *)
(* Means let x = 2 in let y = x + 3;; *)
